#pragma once
#include "libincludes.h"

//void addToSortedList(list *l, int uInput);
void push_sorted(list, int);
void push_sorted_sentinel(list, int);
void addListBegT(list *l, list *tail, int uInput);
